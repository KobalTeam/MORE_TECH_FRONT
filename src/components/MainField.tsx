import React from "react";

const MainField = (): JSX.Element => (
  <div className="main-field">
    <p className="text font-nunito">Все о механизмах начисления</p>
  </div>
);

export default MainField;
