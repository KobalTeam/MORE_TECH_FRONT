import React from "react";

const Search = (): JSX.Element => (
  <div>
    <input
      type="search"
      name="search_header"
      placeholder="Search..."
    />
  </div>
);

export default Search;
